# Local emulator / Cloud Functions runtime config.
#
# Real secrets (API keys for third-party services, etc.) do NOT belong here —
# use Firebase Secret Manager (`firebase functions:secrets:set NAME`) instead.
# This file is only for non-secret runtime configuration values.
# Copy to .env.local for local emulator use (gitignored).

FUNCTIONS_EMULATOR=true
